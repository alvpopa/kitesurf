import React from 'react';
import { Background, RootContainer } from '../components';

const Homepage = () => {
  return (
    <RootContainer>
      <Background />
    </RootContainer>
  );
};

export default Homepage;
