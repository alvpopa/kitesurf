import styled from 'styled-components';

const FilterTooltip = styled.i`
  position: absolute;
  text-align: center;
  align-self: flex-end;
  top: 3rem;
  right: 1rem;
  z-index: 9999;
  cursor: pointer;
  text-transform: uppercase;
`;

export default FilterTooltip;
